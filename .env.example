# Compliant SEC EDGAR User-Agent (name + contact email). Used by Vite dev proxy.
SEC_CONTACT_EMAIL=your.name@company.com

# Optional: verbose SEC step logging in production builds
# VITE_SEC_DEBUG=true

# Use mock SEC data instead of live EDGAR (offline demo)
# VITE_USE_MOCK_SEC_DATA=false

# After npm run dev, verify SEC proxy + revenue (ZS ~2100-2700M, CRWD ~4800M+):
# npm run verify:sec-revenue
